<h2>Are you sure you want to delete this link?</h2>
<p>This action cannot be undone. Press the "Delete Link" button to delete, or click "cancel" to cancel.</p>

<% if @link.active? && (click_count = @link.click_count) > 0 %>
<p><strong>NOTE: This link is active and has been followed <%= click_count %> time<%= 's' if click_count != 1 %>.</strong> </p>
<% end %>

<% form "/admin/links/#{@link.id}", :method => "delete" do %>
  <input type="submit" class="delete" value="Delete Link"> <a href="/admin/links/<%= @link.id %>" class="cancel" onclick="if ( $('#modalContainer').length == 1 ) { closeModal(); return false; }">cancel</a>
<% end %>